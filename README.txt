Instructions:
In terminal, change directory to folder that contains index.js

Afterward, run these commands to ensure neccessary packages are installed:
npm install

Run server, then  go on a browser and type:
localhost:3000

This should take you to the homepage.
Send entries into the database on the FORM page.
View all entries in database on the COLLECTION page.
Query the database on the QUERIES page.